fastapi==0.111.0
uvicorn[standard]==0.30.6
pydantic==2.8.2
httpx==0.27.2
python-dotenv==1.0.1
sqlalchemy==2.0.35
numpy>=1.26
tzdata>=2024.1
# Semantic embeddings (downloads the model on first run; retrieval falls back to BM25 until it loads)
sentence-transformers==3.2.1
# PostgreSQL + PostGIS + pgvector (optional; SQLite is used when DATABASE_URL is sqlite)
psycopg[binary]==3.2.3
pytest==8.3.3
